#! /bin/bash
#
#
#This should be stored in a directory 1 below what you would like served
#
#Uses `livereloadx` as a dependency
#Download with `npm install -g livereloadx` or from http://nitoyon.github.io/livereloadx/
#
#Add `<script src="http://localhost:4200/livereload.js?snipver=2"></script>` to your HTML,
#Or, if you only plan on accessing your site from something other then localhost: `<script>document.write('<script src="http://' + (location.host || 'localhost').split(':')[0] +':4200/livereload.js?snipver=2"></' + 'script>')</script>`

livereloadx -s -p 4200 $( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/..